
<main id="thanks">
    {{>header}}
    <div>
        {{#if count}}
        <label>Signature #{{count}}</label>
        {{/if}}

        {{#if user}}
        <div id="signed" class="canvas">
            <img src="{{user.signature}}">
        </div>
        <h1>Thanks for your support, {{user.first}}!</h1>
        {{/if}}
        <p class="cat"><a href="/petition/signatures">Check out who else signed</a></p>
    </div>
</main>
