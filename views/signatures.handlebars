<main id="supporters">
    <p id="header">make teleportation happen <span>supporters</span></p>
<div>
{{#signers}}
{{#unless @last}}
    <div><span>{{incremented @index}}</span>&emsp;{{first}} {{last}}</div>
    {{/unless}}
    {{#if @last}}
    <div><span>{{incremented @index}}</span>&emsp;<strong>{{first}} {{last}}</strong>&ensp;<div class="identifier">there you are! 😁</div></div>

    {{/if}}
{{/signers}}
</div>
</main>
